#ifndef WORLD_CONF_C
#define WORLD_CONF_C

#include "config/WorldConf.c"
#include <stdlib.h>

WorldSertings* settings_native()
{
     WorldSettings* sertings = (TerrainSettings*)malloc(sizeof(WorldSettings));
     return settings;
}

void set_native_points(WorldSettings* settings, )

#endif
